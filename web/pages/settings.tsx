import React from 'react';

import Settings from '../components/Settings';

const SettingsPage = () => (
  <section>
    <h1>Settings</h1>
    <Settings />
  </section>
);

export default SettingsPage;
